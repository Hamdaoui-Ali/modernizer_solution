import { NewMigrationForm } from "./NewMigrationForm";

export const metadata = {
  title: "New Migration · Migration Factory",
};

export default function NewMigrationPage() {
  return <NewMigrationForm />;
}
