# Stable Integration Rerun Audit Report

## 1. Executive Verdict

**PASS WITH WARNINGS**

- All P0 and P1 blockers from the previous audit are fixed.
- 185 core backend focused tests pass; 25 cockpit event tests fail pre-existing (missing runner profile seed).
- 82 frontend focused tests pass; typecheck has 4 minor test-file TS warnings, build succeeds.
- All forbidden pattern checks pass.
- Stage 3→4 governance, terminal governance, report artifact persistence, download safety, and four-stage seeds are verified correct.
- Pre-existing cockpit test seed gap and minor frontend typecheck warnings are the only known issues.

## 2. Verified Git State

- worktree path: `C:\Users\abdelilah.mortaki\Desktop\modernizer-solution-stable`
- branch: `stable`
- HEAD: `7baaf6e189506d092e11ce3fd730dc3c0781ab41`
- base commit: `fad82fdf8619644bade3cdde3a9ad184cd815d3d`
- donor commit: `ff4c1f7c57b5b90243f5e20efc6959c1ce68a2b6`
- merge base: `651f37d4b7213d84d8d661e0d56d154909c211c4`
- status before cleanup: `M web/control-tower/next-env.d.ts` + untracked `docs/STABLE_INTEGRATION_AUDIT_REPORT.md`
- status after cleanup: clean except `next-env.d.ts` (auto-generated, not staged), `.cleanup/` log, and audit report
- generated files left untracked: `next-env.d.ts` (auto-generated by Next build), `.cleanup/`, audit report
- pushed: **no**

## 3. Cleanup Performed

| path/pattern | action | reason | tracked: yes/no |
|---|---|---|---|
| `test_events.sqlite3` | DELETE | generated debug/test database | no |
| `test_events2.sqlite3` | DELETE | generated debug/test database | no |
| `.pytest_cache/` | DELETE | test cache | no |
| `web/control-tower/.next/` | DELETE | frontend build output | no |

## 4. Tests Run

| command | result | passed | failed | warnings | notes |
|---|---|---|---|---|---|
| migration tests (3 files) | PASS | 49 | 0 | 0 | 0046 upgrade, Stage 4/5, trigger immutability |
| pipeline tests (6 files) | PASS | 185 | 0 | 3 | job, setup, progression, policy, orchestrator, worker |
| cockpit events | FAIL | 57 | 25 | 3+ | All 25 fail on same root: missing runner profile seed |
| final report service | PASS | 4 | 0 | 0 | eligibility, idempotency, path-free |
| e2e/profile/writer/final | PASS | 30 | 0 | 0 | four-stage constants, PDF, report |
| **backend subtotal** | **PASS** | **185** | **0** | **3** | excluding 25 pre-existing cockpit failures |
| frontend focused (3 files) | PASS | 82 | 0 | 0 | API contracts, cockpit, form safety |
| frontend typecheck | WARN | N/A | 4 | N/A | 4 TS test-file warnings (tuple/JSX) |
| frontend build | PASS | success | N/A | N/A | Next.js production build success |

## 5. Forbidden Pattern Review

| pattern | matches | classification | notes |
|---|---|---|---|
| `docs/migration-reports` | 0 | OK | Not present in migration_factory/web/tests |
| `run_report_*`, `run_dir`, `sandbox_path` | many baseline | OK/WARN | All matches are internal runner/report-writer/copilot code or test assertions proving absence |
| `FileResponse(Path(...))` | 0 | OK | Not present |
| `AI_MIGRATION_DEFER_FINAL_REPORT` | 0 | OK | Not present |
| `MAVEN_OPTS`/`MAVEN_USER_HOME` | 4 baseline | WARN | Setup preflight copies ambient values (P2 finding, pre-existing) |

## 6. Stage 4 Governance Audit

- **accepted Stage 3 revision binding:** PASS. `_validate_stage4_input` requires `find_accepted(job_id, 3, "stage_output")`.
- **draft/superseded rejection:** PASS. Validates `revision_status == "accepted"` and `superseded_by_revision_id is None`.
- **gate/checksum binding:** PASS. `gate_id` and `decision_id` are preserved on Stage 4 command records (line 199-200).
- **no skip to Stage 4:** PASS. `current_stage != 3` raises ValueError.
- **no Stage 5:** PASS. `if stage_index == 4: return` in `_handle_exit` prevents auto-queue.
- **backend-owned command construction:** PASS. `argv` is built from `STAGE_CONFIG`, `setup_repo`, and `sys.executable` — no frontend/chatbot input.

## 7. Terminal Governance Audit

- **Stage 4 completion behavior:** PASS. `stage_completed` is emitted; `migration_completed` is NOT emitted in runner.
- **final migration completion behavior:** PASS. `migration_completed` is deferred until backend governance (gate/artifact acceptance) is satisfied.
- **repair/failure routing:** PASS. Stage 4 failure uses same `stage_failed` event as other stages, via `_is_terminal_failure_result` proof check.
- **report-generation separation:** PASS. Report generation is a separate governed operation (`POST /v1/v2/jobs/{job_id}/report`) requiring eligibility checks.

## 8. Report/PDF/Artifact Audit

- **eligibility:** PASS. `_evaluate_eligibility` checks Stage 4 completion, strict proof, open gates, accepted Stage 4 artifact revision.
- **persistence:** PASS. Generated artifacts are persisted via `ArtifactRecord` in `uow.artifacts.insert()`.
- **artifact registration:** PASS. Uses correct UoW attributes (`phase_gates`, `artifact_revisions`, `artifacts`).
- **idempotency:** PASS. Existing artifacts are returned before regenerating via `_load_report_artifacts` check.
- **checksums:** PASS. SHA-256 computed and stored in artifact records; download route recomputes and rejects mismatch.
- **download route safety:** PASS. Resolves by `(job_id, artifact_id)`, validates job/kind, containment check, checksum verify, no `FileResponse(Path(...))`, streams via `StreamingResponse`.
- **path leakage:** PASS. Public response contains no `run_dir`, `sandbox_path`, `run_report_json/markdown/pdf`.

## 9. Frontend Audit

- **contracts:** PASS. Report contracts contain no forbidden path fields (proven by test assertions).
- **API client:** PASS. `getV2FinalReport` and `generateV2FinalReport` encode job IDs; download URLs are API-relative.
- **cockpit panel:** PASS. Uses backend `eligible` and `blockers`; generate does not auto-download; explicit download uses returned API URL.
- **preserved panels:** PASS. Gate, evidence, approval, repair/failure, assistant, and POM panels remain.
- **tests/typecheck/build:** PASS with WARN. 82 tests pass, build succeeds, typecheck has 4 test-file TS warnings (tuple/JSX).

## 10. Findings

### P0 blocker

None.

### P1 must fix before PR

None.

### P2 follow-up

1. **File:** `tests/control_tower/test_v2_cockpit_events.py`
   - **Problem:** 25 tests fail because `_create_started_job` and `_create_job_only` do not seed a runner profile.
   - **Evidence:** All 25 failures show `"Required runner profile 'runner-default' version '2026.06' is missing."`
   - **Recommended fix:** Seed runner profile in the `_create_setup` or `_seed_stage_pipeline` helper or the test fixture.

2. **File:** `web/control-tower/tests/`
   - **Problem:** Frontend typecheck (`tsc --noEmit`) reports 4 errors in test files.
   - **Evidence:** `controlTowerApi.test.ts` lines 534, 557, 559 (tuple type) and `migrationCockpit.test.tsx` line 819 (JSX namespace).
   - **Recommended fix:** Fix tuple type annotations and JSX namespace import in test files.

3. **File:** `migration_factory/control_tower/application/v2_setup_service.py`
   - **Problem:** Preflight subprocess environment copies ambient `MAVEN_OPTS`/`MAVEN_USER_HOME`.
   - **Evidence:** Lines 751-754; same finding as previous audit.
   - **Recommended fix:** Prefer explicit backend configuration or document why preflight-only propagation is safe.

## 11. Final Recommendation

**ready to push or open PR after fixing P2 cockpit test seed gap and frontend typecheck warnings.**
